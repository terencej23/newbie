# output hello world
define function hello_world with params (x)
	return x

# for now we require a main function as entrypoint - parser cant recognize free statements
define function main with no params
	y = hello_world(2)
